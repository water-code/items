<!--Searcher/searchShow/searchShow.wxml-->
<van-card
  wx:for="{{goodsList}}"
  wx:key="goods_id"
  num="{{item.goods_number}}"
  price="{{item.goods_price}}"
  title="{{item.goods_name}}"
  thumb="{{ item.goods_small_logo }}"
  bindtap="gotoDetails"
  data-id="{{item.goods_id}}"
/>
